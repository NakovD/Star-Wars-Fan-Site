import React from 'react';
import styles from './NoChars.module.css';


const NoChars = () => {

    return (
        <div className={styles.noChars}>
            There aren't any characters that match your search!
        </div>
    )
}

export default NoChars;